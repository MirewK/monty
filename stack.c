#include "monty.h"
/**
 * stack_fun - sets the format of the data to a stack(LIFO)
 * @stack: pointer to the head of the stack
 * @line_number: stack line number to the element
 *
 * Return: nothing
 */
void stack_fun(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	data.d_flag = 0;
}
