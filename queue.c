#include "monty.h"
/**
 * queue_fun - sets the format of the data to a queue(FIFO)
 * @stack: pointer to the head of the queue
 * @line_number: queue line number of an element
 *
 * Return: nothing
 */
void queue_fun(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
	data.d_flag = 1;
}
